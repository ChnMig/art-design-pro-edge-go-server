# 艺术设计专业版 Go 服务器环境变量模板
# 复制此文件到 .env 并更新为您环境的值
# 
# 重要提示:
# - 请勿将 .env 文件提交到版本控制
# - 在生产环境中使用强密码、唯一值
# - 定期轮换密钥

# 服务器配置
SERVER_PORT=8080

# JWT 配置
# 生成强随机密钥: openssl rand -base64 32
JWT_KEY=请在此处填入您的超级机密jwt密钥并在生产环境中更改
JWT_EXPIRATION=12h

# Redis 配置
REDIS_HOST=127.0.0.1:6379
REDIS_PASSWORD=请填入您的redis密码

# PostgreSQL 配置
POSTGRES_HOST=127.0.0.1
POSTGRES_USER=postgres
POSTGRES_PASSWORD=请填入您的postgres密码
POSTGRES_DBNAME=server
POSTGRES_PORT=5432
POSTGRES_SSLMODE=disable
POSTGRES_TIMEZONE=Asia/Shanghai

# 管理员配置
# 为管理员用户使用强密码
ADMIN_PASSWORD=请填入您的管理员密码
# 生成强盐值: openssl rand -base64 32
PASSWORD_SALT=请填入您的密码盐值

# 安全说明:
# 1. JWT_KEY 应至少32个字符且为密码学随机数
# 2. PASSWORD_SALT 应唯一且绝不分享
# 3. 所有密码都应遵循强密码策略
# 4. 在生产环境中，考虑使用密钥管理服务

# 常见生成密钥命令:
# JWT 密钥 (32+ 字符): openssl rand -base64 32
# 密码盐值 (32+ 字符): openssl rand -base64 32
# 强密码: openssl rand -base64 24